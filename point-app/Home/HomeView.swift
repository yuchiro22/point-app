import SwiftUI

struct HomeView: View {
    var body: some View {
        Text("ホーム画面")
            .padding()
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
