import SwiftUI

struct CouponView: View {
    var body: some View {
        Text("クーポン画面")
    }
}

struct CouponView_Previews: PreviewProvider {
    static var previews: some View {
        CouponView()
    }
}
